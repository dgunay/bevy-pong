use std::time::Duration;

/// How hard the shakes effect is by default.
pub const DEFAULT_SHAKE_INTENSITY: f32 = 25.0;
/// How long the shakes last by default.
pub const DEFAULT_SHAKE_DURATION: Duration = Duration::from_millis(500);
